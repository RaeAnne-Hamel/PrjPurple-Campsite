package campground_data;

import java.util.HashMap;
import java.util.UUID;

/***
 * example controller- may remove
 */
public class GuestController {

/*    private HashMap<Integer, Guest> guests;
    private ValidationHelper vh;

    public GuestController(HashMap<Integer, Guest> guests, ValidationHelper vh) {
        this.guests = guests;
        this.vh = vh;
    }*/

    /***
     * Add car as long as it's valid and not in the hashmap already
     * @param guest - guest object to be added to hash map
     * @return Guest | null - if successful return original guest object - if failed return null
     */
/*    public Guest addGuest(Guest guest){
        // ensure guest is not in guest and double check guest is valid
        if(!guests.containsKey(guest.getGuestID()) && vh.isValid(guests)) {
            guests.put(guest.getGuestID(), guest);
            return guest;
        }else{
            return null; //indicate operation failed
        }
    }*/
}
